#!/usr/bin/env ruby

require "bundler/setup"
#do i need this here? )

require "irb"
require "pry"


def reload
	load_all './lib'
end 

require_relative "../config/environment"

Pry.start
IRB.start


puts "welcome to the sandbox"

#use this template
